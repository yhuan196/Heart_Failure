---
title: "EDA"
author: "Runze Cui"
date: "2023-11-10"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, message = FALSE, warning = FALSE)
library(tidyverse)
library(knitr)
library(kableExtra)
library(survival)
library(writexl)
library(readxl)
```

# Read File
```{r}
dat <- read_csv("./data/heart_failure.csv") %>% 
  arrange(TIME) %>% janitor::clean_names() %>% 
  mutate(gender = factor(gender), 
         smooking = factor(smoking),
         diabetes = factor(diabetes),
         bp = factor(bp),
         anaemia = factor(anaemia))
# write_xlsx(dat, "./data/dat.xlsx")
dat 
```
# EDA

```{r}
dat %>% as.tibble()
summary(dat)
sum(is.na(dat))
```
Discrete Variable: event, gender, smoking, 

Continuous Variable: 

```{r}
# create a table:
```



# Life Table

# K-M Model

# Log-Rank Test

# Cox PH Model